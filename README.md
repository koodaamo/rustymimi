# rustymimi

Python bindings for the Mimi audio codec used by Moshi.

## Build

Build the package and its Python 3.14 wheel with:

```bash
uv build
```

The resulting wheel targets Apple Silicon macOS and includes the Rust extension.
